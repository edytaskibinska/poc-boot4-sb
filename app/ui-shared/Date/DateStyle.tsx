import styled from '@emotion/styled'
import vars from "../../globalStyles/variables"

const DateStyled = styled.sub`
    text-align: right;
    color: ${vars.color.yellow};
`
export default DateStyled